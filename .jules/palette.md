## 2026-08-26 - Explicit Visual Indicators for Truncated Arrays
**Learning:** When truncating lists in constrained terminal UIs (like maxVisible tasks), users lose situational awareness if there is no explicit visual indicator of hidden items.
**Action:** Always include a visual counter (e.g. '... and X more') when array counts exceed visibility limits in ink components to preserve context and accessibility.
