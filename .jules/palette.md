## 2024-07-11 - CLI Screen Reader Accessibility
**Learning:** Screen readers in CLI environments rely on raw text output and fail to announce symbols like `↑↓` or `⏎` properly, causing accessibility regressions.
**Action:** Use visually concise but screen-reader-friendly descriptive navigation words (like 'Up/Down', 'Enter') instead of purely symbolic text for CLI UI components.
