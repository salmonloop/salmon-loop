## YYYY-MM-DD - Active Task Feedback
**Learning:** Users lack immediate visual feedback on which background tasks (like TODOs) are actively running in CLI interfaces since static text like `[/]` doesn't draw the eye.
**Action:** Use animated components like `ink-spinner` for active/in-progress states to clearly indicate background activity without relying solely on color or text changes.