## 2026-08-07 - Dynamic List Truncation Awareness
**Learning:** Truncating dynamic lists (like TODOs) without explicit indicators causes users to lose situational awareness of remaining items.
**Action:** Always include an explicit visual indicator (like "... and X more tasks") when a terminal UI list is truncated to fit constraints.
