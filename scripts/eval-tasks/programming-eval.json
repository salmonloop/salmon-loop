{
  "cases": [
    {
      "id": "fibonacci-memo",
      "file": "eval-tasks/fibonacci-memo/task.json",
      "instruction": "Write a Python file `fibonacci.py` in the current directory that implements a memoized Fibonacci function. The file should export a function `fib(n: int) -> int` that returns the n-th Fibonacci number (0-indexed, fib(0)=0, fib(1)=1). It should handle n up to at least 1000 efficiently using memoization."
    },
    {
      "id": "csv-filter",
      "file": "eval-tasks/csv-filter/task.json",
      "instruction": "Write a Python file `csv_filter.py` in the current directory. It should define a function `filter_rows(input_path: str, output_path: str, column: str, min_value: float) -> int` that reads a CSV file, keeps only rows where the specified column's value is >= min_value, writes the filtered rows to output_path, and returns the number of rows written. Handle missing files gracefully by returning -1."
    },
    {
      "id": "matrix-ops",
      "file": "eval-tasks/matrix-ops/task.json",
      "instruction": "Write a Python file `matrix_ops.py` in the current directory that implements matrix operations WITHOUT using numpy. Define: `mat_mul(a: list[list[float]], b: list[list[float]]) -> list[list[float]]` for matrix multiplication, and `mat_transpose(a: list[list[float]]) -> list[list[float]]` for transpose. Raise ValueError for dimension mismatches."
    },
    {
      "id": "http-status-histogram",
      "file": "eval-tasks/http-status-histogram/task.json",
      "instruction": "Write a Python file `http_histogram.py` in the current directory. Define `parse_log(log_text: str) -> dict[int, int]` that parses HTTP access log lines (Apache combined format) and returns a dictionary mapping HTTP status codes to their occurrence count. Lines that can't be parsed should be skipped."
    },
    {
      "id": "bst-operations",
      "file": "eval-tasks/bst-operations/task.json",
      "instruction": "Write a Python file `bst.py` in the current directory implementing a Binary Search Tree. Define a class `BST` with methods: `insert(val: int)`, `search(val: int) -> bool`, `inorder() -> list[int]` (returns sorted list), and `min_val() -> int` (raises ValueError if empty). No external libraries."
    },
    {
      "id": "rate-limiter",
      "file": "eval-tasks/rate-limiter/task.json",
      "instruction": "Write a Python file `rate_limiter.py` implementing a token bucket rate limiter. Define class `RateLimiter` with `__init__(self, rate: float, capacity: int)` where rate is tokens per second and capacity is max burst size. Method `allow() -> bool` returns True if a request is allowed, False if rate limited. Use time-based logic, not sleep."
    },
    {
      "id": "json-path-getter",
      "file": "eval-tasks/json-path-getter/task.json",
      "instruction": "Write a Python file `json_path.py` in the current directory. Define `get_nested(data: dict, path: str, default=None) -> Any` that retrieves a value from a nested dict using dot notation path like 'a.b.c'. If any key in the path doesn't exist, return default. Support array indices like 'a.0.b' for list access."
    },
    {
      "id": "markdown-table-parser",
      "file": "eval-tasks/markdown-table-parser/task.json",
      "instruction": "Write a Python file `md_table.py` in the current directory. Define `parse_table(md_text: str) -> list[dict[str, str]]` that parses a Markdown table (with header row, separator row, and data rows) into a list of dictionaries keyed by column headers. Return empty list for invalid input."
    },
    {
      "id": "lru-cache",
      "file": "eval-tasks/lru-cache/task.json",
      "instruction": "Write a Python file `lru_cache.py` implementing an LRU cache. Define class `LRUCache` with `__init__(self, capacity: int)`, `get(key: str) -> Optional[Any]` (returns None if not found), and `put(key: str, value: Any) -> None` (evicts least recently used when capacity exceeded). O(1) for both operations."
    },
    {
      "id": "text-justifier",
      "file": "eval-tasks/text-justifier/task.json",
      "instruction": "Write a Python file `justifier.py` in the current directory. Define `justify(text: str, width: int) -> str` that takes a paragraph and returns it with each line (except the last) justified to exactly `width` characters by distributing extra spaces between words. Last line should be left-justified. Words should not be broken."
    },
    {
      "id": "priority-queue",
      "file": "eval-tasks/priority-queue/task.json",
      "instruction": "Write a Python file `priority_queue.py` in the current directory. Implement a thread-safe priority queue using `heapq` and `threading`. Define class `PriorityQueue` with:\n- `__init__(self, mode='max')` — 'max' returns highest priority first, 'min' returns lowest\n- `put(self, item, priority)` — O(log n) insertion\n- `get(self, timeout=None) -> item` — blocks if empty, raises exception on timeout expiry\n- `peek(self) -> item` — returns next item without removing\n- `__len__(self) -> int`\nAll operations must be thread-safe using locks. Use `threading.Condition` for blocking get."
    },
    {
      "id": "connection-pool",
      "file": "eval-tasks/connection-pool/task.json",
      "instruction": "Write a Python file `connection_pool.py` in the current directory. Implement a generic connection pool. Define class `ConnectionPool` with:\n- `__init__(self, factory, max_size, min_size=0, max_idle_time=None, health_check=None)` — factory is a callable that creates connections, health_check is an optional callable(conn)->bool\n- `acquire(self, timeout=None) -> context manager` — returns a connection; blocks if pool is full until timeout\n- `release(self, conn)` — returns connection to pool; closes it if idle time exceeded\n- `close(self)` — closes all connections and prevents further acquire\nThread-safe. Use `threading.Condition` for waiting. Support context manager protocol on acquired connections."
    },
    {
      "id": "trie-autocomplete",
      "file": "eval-tasks/trie-autocomplete/task.json",
      "instruction": "Write a Python file `trie.py` in the current directory. Implement a trie-based autocomplete engine. Define class `Trie` with:\n- `insert(self, word, weight)` — insert word with numeric weight\n- `search(self, prefix) -> list[str]` — return all words matching prefix, sorted by weight descending\n- `delete(self, word) -> bool` — remove word, return True if existed\n- `autocomplete(self, prefix, k) -> list[str]` — top-k matches by weight\n- `bulk_insert(self, words)` — words is list of (word, weight) tuples\n- `save(self, path)` — serialize to JSON file\n- `@staticmethod load(path) -> Trie` — deserialize from JSON file\nMust handle 100k words efficiently (bulk_insert < 3s)."
    },
    {
      "id": "expr-eval",
      "file": "eval-tasks/expr-eval/task.json",
      "instruction": "Write a Python file `expr_eval.py` in the current directory. Implement a recursive-descent expression parser and evaluator. Define:\n- `parse(expr: str) -> AST` — parse into AST node objects\n- `evaluate(expr: str, context=None) -> float` — parse and evaluate\nSupport: `+`, `-`, `*`, `/`, `%`, `**` (right-associative), parentheses, unary negation, variables via context dict, function calls (`abs`, `min`, `max`, `sqrt` as builtins). Raise `SyntaxError` for bad syntax, `ZeroDivisionError` for division by zero, `NameError` for undefined variables."
    },
    {
      "id": "task-scheduler",
      "file": "eval-tasks/task-scheduler/task.json",
      "instruction": "Write a Python file `task_scheduler.py` in the current directory. Implement a DAG-based task scheduler with concurrent execution. Define class `TaskScheduler` with:\n- `add_task(self, task_id, fn, deps=None)` — fn is a callable, deps is list of task_id strings\n- `run(self) -> dict[str, dict]` — execute all tasks respecting dependencies, return {task_id: {'status': 'ok'|'error', 'value': ..., 'error': ...}}\nUse `concurrent.futures.ThreadPoolExecutor` — independent tasks must run in parallel. Detect circular dependencies and raise `ValueError`. On task failure, mark all downstream dependents as error without executing them."
    }
  ]
}
