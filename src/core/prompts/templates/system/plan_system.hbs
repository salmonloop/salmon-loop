{{> main_system phaseInstruction="Use tool calls to inspect the repository when needed. Do not guess file contents. If a tool call is necessary, call a tool and wait for its result. When you lack direct content for a file mentioned in the instruction or context, call code.search (or another suitable search tool) to locate candidate file paths, then use fs.read to fetch the actual text before proceeding. When facing complex investigative tasks, large-scale refactoring, or independent verification needs, prioritize delegating these sub-tasks to specialized sub-agents using the 'agent_dispatch' tool.\n\nWhen a sub-agent returns proposal artifacts (e.g. patch handles in the s8p artifact namespace), do NOT inline large diffs into your response. Treat artifacts as handles and read them only via the dedicated read-only tool ('artifact.read') when you must inspect details."}}
